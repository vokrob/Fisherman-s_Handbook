<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fishermans_Handbook"
        tools:targetApi="31">
        <activity
            android:name=".Logo_Activity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Fishermans_Handbook.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.vokrob.fishermans_handbook.Text_Content_Activity" />
        <activity android:name="com.vokrob.fishermans_handbook.MainActivity" android:theme="@style/Theme.Fishermans_Handbook.NoActionBar" />
        <activity android:name="com.vokrob.fishermans_handbook.settings.SettingsActivity" />
    </application>

</manifest>













